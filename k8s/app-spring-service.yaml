apiVersion: v1
kind: Service
metadata:
  name: app-spring
spec:
  selector:
    app: app-spring
  ports:
    - protocol: TCP
      port: 8099
      targetPort: 8099
      nodePort: 30099
  type: NodePort